# Tests

Numerical reconstruction, consistency, and artifact/source checks belong here.

The final archived release should preserve the exact test suite used to verify the frozen evidence package and should record the command needed to execute it from a clean environment.
