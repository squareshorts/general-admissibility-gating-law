# Configuration

Frozen detector, gate, threshold, partition, preprocessing, simulation, and cost-scenario settings belong here.

For the manuscript release, configuration files should be versioned and treated as immutable evidence of the prespecified analysis state.
